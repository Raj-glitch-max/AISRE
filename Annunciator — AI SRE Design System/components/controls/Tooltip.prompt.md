Floating readout for a value that needs one extra fact — the only overlay-fill surface.

```jsx
<Tooltip content="p95 latency, last 5 min">
  <MetaLabel>2,410 ms</MetaLabel>
</Tooltip>
```

Appears instantly on hover or focus — no fade, no delay. Keep the content to a single line of data-sm.
