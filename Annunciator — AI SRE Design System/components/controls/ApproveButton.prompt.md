The only button in the system — it answers the caution light.

```jsx
<ApproveButton onClick={approve}>APPROVE RESTART</ApproveButton>
```

Hover brightens the fill to #ffc247 with no transition, no lift, no shadow. Disabled drops to the overlay fill with grey text. Do not create a secondary or ghost variant.
