Hairline rule between list rows and panel sections — the only separator in the system.

```jsx
<IncidentRow />
<Divider />
<IncidentRow />
```

Use `inset` only when a row's own padding should visibly continue past the rule. Never use Divider to build a box outline.
